### This file is managed by Puppet.  Any changes will be overwritten
##
##

;MGM_IP should have the managment node (web server) IP address
; it is used in extensions.conf to send email notifications
; by the email notifier script
MGM_IP="<%= @ast_mgm01_ip %>"   ; managment node (web server) IP address

; NOTIFY_EMAIL_ON_ERROR is used by the email notifier script when an exception is
; raised in Asterisk.
NOTIFY_EMAIL_ON_ERROR="<%= @ast_notification_email %>"  ; enter the recipient email to receive error notifications
